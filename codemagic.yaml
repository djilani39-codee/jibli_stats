workflows:
  ios-workflow:
    name: Jibli Stats iOS Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      groups:
        - apple_auth 
      flutter: stable
      xcode: latest
    scripts:
      - name: Get Flutter dependencies
        script: |
          flutter pub get
      - name: Set up code signing
        script: |
          keychain initialize
          # تأكد أن المعرف أدناه هو نفسه الذي اخترته في موقع Apple
          app-store-connect fetch-signing-files "com.jibli_stats" --publish-type app-store --create
          export_signing_files
      - name: Build iOS IPA
        script: |
          flutter build ios --release --build-number=$BUILD_NUMBER
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration  # هذا السطر وحده يكفي الآن
      email:
        recipients:
          - djilanibenhacine5@gmail.com