# Arabic PDF Font Asset

Place a TrueType font file that supports Arabic glyphs in this folder.

Recommended file name:
- `Dubai-Regular.ttf`

Then run:
```bash
flutter pub get
```

The Flutter app will load this font for PDF generation and avoid missing Arabic glyphs.
